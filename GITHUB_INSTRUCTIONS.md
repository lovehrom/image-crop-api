# GitHub репозиторий — Инструкция

## Что уже сделано ✅

- ✅ Git инициализирован в проекте
- ✅ `.gitignore` создан и настроен
- ✅ Все файлы добавлены в git
- ✅ Первый коммит создан

---

## 🚀 Шаг 1: Создание репозитория на GitHub

### Вариант A: Через сайт GitHub (самый простой)

1. Зайди на https://github.com
2. Нажми на **"+"** в правом верхнем углу
3. Выбери **"New repository"**
4. Заполни поля:
   - **Repository name:** `image-crop-api`
   - **Description:** `Professional image cropping API with rotation and border radius support. Fast, simple, and reliable.`
   - **Visibility:** ✅ Public (для RapidAPI нужен public репозиторий)
   - **Инициализация:** ❌ НЕ выбирай "Add a README file", "Add .gitignore", "Choose a license" (мы уже это сделали)
5. Нажми **"Create repository"**

### Вариант B: Через GitHub CLI (если установлен)

```bash
gh repo create lovehrom/image-crop-api --public --description "Professional image cropping API with rotation and border radius support"
```

---

## 📤 Шаг 2: Подключение удалённого репозитория

После создания репозитория на GitHub, скопируй его URL (он будет показан на странице):
- URL будет вида: `https://github.com/lovehrom/image-crop-api.git`

Теперь подключи его к локальному проекту:

```bash
cd K:\Работа\rapidapi.com\image-crop-api
git remote add origin https://github.com/lovehrom/image-crop-api.git
```

---

## 📤 Шаг 3: Пуш на GitHub

```bash
# Пуш первой ветки (main)
git branch -M main
git push -u origin main
```

Если попросит логин:
- Username: `lovehrom`
- Password: **твой Personal Access Token** (НЕ обычный пароль!)

---

## 🔑 Если нужна аутентификация

### Создание Personal Access Token (если ещё нет)

1. Зайди на https://github.com/settings/tokens
2. Нажми **"Generate new token (classic)"**
3. Заполни поля:
   - **Note:** `image-crop-api`
   - **Expiration:** `No expiration` (или выбери срок)
   - **Scopes:** ✅ `repo` (или ✅ `public_repo` только для публичных репозиториев)
4. Нажми **"Generate token"**
5. **Скопируй токен** (он будет показан только один раз!)

### Использование токена

```bash
# При пуше GitHub попросит логин
Username: lovehrom
Password: ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx  (твой токен)
```

---

## ✅ После успешного пуша

GitHub покажет URL репозитория:
```
https://github.com/lovehrom/image-crop-api
```

Этот URL будет нужен для RapidAPI!

---

## 📊 Что в репозитории

### Исходный код
- `index.js` — точка входа Express
- `src/` — весь код (controllers, services, routes, middleware, utils)
- `package.json` — зависимости

### Документация
- `README.md` — описание API
- `PROJECT_STATUS.md` — статус проекта
- `FINAL_TEST_REPORT.md` — финальный отчёт о тестировании
- `TEST_REPORT.md` — краткий отчёт
- `TEST_REPORT_AUTONOMOUS.md` — детальный отчёт тестов

### Тестирование
- `tester.js` — автономный тестер
- `create-test-image.js` — создание тестового изображения
- `generate-examples.js` — создание примеров

### Конфигурация
- `vercel.json` — для деплоя на Vercel
- `.gitignore` — исключения Git
- `.env.example` — пример переменных окружения

---

## 🎯 Следующие шаги

### 1. Деплой на Vercel

```bash
cd K:\Работа\rapidapi.com\image-crop-api

# Если Vercel CLI не установлен:
npm i -g vercel

# Деплой:
vercel deploy
```

### 2. Получение URL

Vercel выдаст URL вида:
```
https://image-crop-api.vercel.app
```

### 3. Публикация на RapidAPI

- В RapidAPI Studio добавь GitHub URL:
  `https://github.com/lovehrom/image-crop-api`

---

## 🔧 Если что-то не работает

### Ошибка: "remote origin already exists"

```bash
# Удали старый origin
git remote remove origin

# Добавь новый
git remote add origin https://github.com/lovehrom/image-crop-api.git
```

### Ошибка: "Authentication failed"

- Используй Personal Access Token вместо пароля
- Проверь, что токен имеет доступ `repo` или `public_repo`

### Ошибка: "push rejected"

```bash
# Проверь статус
git status

# Если есть изменения, закоммить их
git add .
git commit -m "Update files"
git push -u origin main
```

---

## 📝 Полезные команды

```bash
# Статус изменений
git status

# История коммитов
git log --oneline

# Просмотр удалённых репозиториев
git remote -v

# Удаление файла из git (но не с диска)
git rm --cached имя_файла
git commit -m "Remove file"

# Удаление коммита (отмена последнего коммита)
git reset --soft HEAD~1
```

---

## 💡 Советы

1. **Создай README.md на GitHub** — красивое описание с логотипом привлечёт больше пользователей
2. **Добавь LICENSE** — MIT лицензия позволяет всем использовать код свободно
3. **Добавь Issues** — включи Issues, чтобы пользователи могли сообщать о багах
4. **Добавить Discussions** — для вопросов и обсуждений
5. **GitHub Actions** — автоматические тесты при каждом коммите (если хочешь)

---

**Готово!** После выполнения этих шагов проект будет на GitHub и готов к деплою на Vercel.
