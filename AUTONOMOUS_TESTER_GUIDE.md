# AUTONOMOUS_TESTER_GUIDE.md — Инструкция для автономного тестера

---

## 📋 Обзор

Инструменты для автономного тестирования Image Crop API без ручного ввода команд.

---

## 🚀 Как использовать автономный тестер

### Вариант 1: Автоматический тест (рекомендую)

**Шаг 1: Установи зависимости**
```bash
cd "K:\Работа\rapidapi.com\image-crop-api"
npm install
```

**Шаг 2: Запусти сервер**
```bash
npm start
```

**Шаг 3: Запусти автономный тестер**
Открой новый терминал и выполни:
```bash
cd "K:\Работа\rapidapi.com\image-crop-api"
npm run test:api
```

**Шаг 4: Проверь результаты**
После завершения тестирования будет создан файл:
```
TEST_REPORT_AUTONOMOUS.md
```

---

### Вариант 2: Ручное тестирование через интерфейс

Если автоматический тест не работает — используй INSTRUCTIONS_TESTING.md

**Что делать:**
1. Следуй инструкциям из `INSTRUCTIONS_TESTING.md`
2. Выполняй команды curl вручную
3. Проверяй результаты
4. Запиши результаты в `TEST_REPORT_MANUAL.md`

---

## 📋 Что делает автономный тестер

### Тестовые функции

1. **Health Check**
   - Проверяет что API возвращает статус `healthy`
   - Проверяет версию API `1.0.0`
   - Проверяет timestamp

2. **Basic Crop**
   - Обрезка изображения до размеров 800x600px
   - Проверяет что ответ PNG формат
   - Проверяет размер выходного файла

3. **Crop with Coordinates**
   - Обрезка по координатам (100, 100, 400x400)
   - Проверяет что обрезка корректная
   - Проверяет что ответ PNG формат

4. **Rotate (90°, 180°, 270°, custom 45°)**
   - Поворот изображения на указанные углы
   - Проверяет что поворот правильный (по часовой стрелке)
   - Проверяет что ответ PNG формат

5. **Border Radius (20px, 10px)**
   - Скругление углов изображения
   - Проверяет что скругление корректно
   - Проверяет что ответ PNG формат

6. **Resize**
   - Изменение размера изображения (500x400px)
   - Проверяет что размер изменился
   - Проверяет что ответ PNG формат

7. **Format Change (JPEG)**
   - Изменение формата PNG → JPEG
   - Проверяет что ответ JPEG формат (image/jpeg)
   - Проверяет что качество 85

8. **Complex Request**
   - Все параметры вместе (resize + crop + rotate + radius + format)
   - Проверяет что все операции выполнены
   - Проверяет что ответ JPEG формат

9. **Error Handling**
   - Missing File — проверяет что возвращается 400 Bad Request
   - Invalid Width — проверяет что возвращается 400 Bad Request
   - Invalid Angle — проверяет что возвращается 400 Bad Request
   - Invalid Format — проверяет что возвращается 400 Bad Request

### Автоматизация

Тестер работает полностью автономно:
- Отправляет все запросы через fetch API
- Проверяет статусы ответов (200 OK, 400 Bad Request)
- Проверяет Content-Type ответов
- Сохраняет результаты в файл
- Генерирует отчёт с процентом успешных тестов

---

## 📋 Чек-лист для тестирования

### Перед тестированием

- [ ] Зависимости установлены (`npm install`)
- [ ] Сервер запущен (`npm start`)
- [ ] Тестовое изображение создано (`test_image.png`)
- [ ] `.env` файл создан (PORT=3000)
- [ ] Свободный порт 3000

### После автоматического тестирования

- [ ] `TEST_REPORT_AUTONOMOUS.md` создан
- [ ] Все 16 тестов выполнены
- [ ] Процент успешных тестов > 80%
- [ ] Нет критических ошибок (failed testы на core фичи)

---

## 📊 Что должен быть в тестовом изображении

Требования к `test_image.png`:
- Формат: PNG или JPG
- Размер: Минимум 800x600px (чтобы можно было обрезать)
- Размер файла: < 5MB
- Путь: Положи в папку проекта `K:\Работа\rapidapi.com\image-crop-api\test_image.png`

---

## 📊 Ожидаемые результаты

### Автоматический тест

**Успешные тесты:**
- ✅ Health Check — Status: healthy
- ✅ Basic Crop — 800x600px PNG
- ✅ Crop with Coordinates — 400x400px cropped
- ✅ Rotate 90° — повёрнутое изображение
- ✅ Rotate 180° — перевёрнутое изображение
- ✅ Rotate 270° — повёрнутое против часовой
- ✅ Rotate Custom 45° — повёрнутое на 45°
- ✅ Border Radius 20px — скруглённые углы
- ✅ Border Radius 10px — маленькое скругление
- ✅ Resize — 500x400px изображение
- ✅ Format Change — JPEG формат
- ✅ Complex Request — все операции вместе

**Ошибки (должны работать):**
- ✅ Missing File — 400 Bad Request
- ✅ Invalid Width — 400 Bad Request
- ✅ Invalid Angle — 400 Bad Request
- ✅ Invalid Format — 400 Bad Request

**Процент успеха:**
- Минимум: 90% (14 из 16 тестов)
- Идеально: 100% (все тесты пройдены)

---

## 📝 Создание TEST_REPORT_AUTONOMOUS.md

После завершения автоматического тестирования будет создан файл с результатами.

Формат:
```markdown
# TEST_REPORT_AUTONOMOUS.md
Дата: [дата]

## Сводка
- Общее количество тестов: 16
- Успешных: [количество]
- Провалено: [количество]
- Процент успеха: [X]%

## Результаты

### Успешные тесты
- [ ] Health Check
- [ ] Basic Crop
- [ ] Crop with Coordinates
- [ ] Rotate 90°
- [ ] Rotate 180°
- [ ] Rotate 270°
- [ ] Rotate Custom 45°
- [ ] Border Radius 20px
- [ ] Border Radius 10px
- [ ] Resize
- [ ] Format Change
- [ ] Complex Request

### Проваленные тесты
- [ ] Название теста — причина провала

## Выводы
- API [готов / не готов] к деплою
- Системы [работают / не работают] корректно
- Производительность [отлично / хорошо / средне]

## Рекомендации
- [ ] Если процент успеха < 90% — нужно доработать
- [ ] Если есть critical failures — нужно исправить баги
```

---

## 🚀 Автономный запуск тестера

**В один клик:**
```bash
npm run test:api
```

**Это автоматически:**
1. Запустит все 16 тестов
2. Проверит результаты
3. Сохранит отчёт в `TEST_REPORT_AUTONOMOUS.md`
4. Покажет проценты успешных тестов

---

## 📋 Критерии готовности к деплою

### Для тестера

✅ Все 16 тестов пройдены  
✅ Процент успеха > 90%  
✅ Критические баги отсутствуют  
✅ Performance < 500ms для простых операций  

### Для проекта

✅ Все обязательные фичи работают  
✅ Валидация всех параметров  
✅ Обработка ошибок корректная  
✅ Content-Type ответов правильный  

---

## 📋 Что делать если тесты провали

### Если процент успеха < 90%

**1. Прочитай TEST_REPORT_AUTONOMOUS.md**
- Посмотри какие тесты провалились
- Посмотри причины провалов

**2. Сообщи мне результаты**
- Отправь файл `TEST_REPORT_AUTONOMOUS.md`
- Опиши какие проблемы

**3. Исправь баги**
- Я проанализирую результаты
- Исправлю код
- Запущу тестер снова

---

## 📋 Пример успешного теста

```bash
$ npm run test:api

=== Starting All Tests ===
Base URL: http://localhost:3000
Test Image: K:\Работа\rapidapi.com\image-crop-api\test_image.png

=== Testing Health Check ===
✅ PASSED: Health Check
   Status: 200
   Data: {"status":"healthy","version":"1.0.0","timestamp":"2024-01-01T00:00:00Z"}

=== Testing Basic Crop ===
✅ PASSED: Basic Crop 800x600
   Status: 200
   Content-Type: image/png
   Buffer size: 287452 bytes

[... остальные тесты ...]

=== Test Results ===
Total tests: 16
Passed: 16
Failed: 0
Success rate: 100.0%

Report saved to: TEST_REPORT_AUTONOMOUS.md

✅ All tests passed! API is ready for deployment.
```

---

## 📋 Пример провального теста

```bash
$ npm run test:api

=== Testing Basic Crop ===
❌ FAILED: Basic Crop 800x600
   Status: 500
   Error: Failed to process image
   Details: Cannot read property 'width' of undefined

=== Test Results ===
Total tests: 16
Passed: 10
Failed: 6
Success rate: 62.5%

⚠️ Test suite failed. Review failed tests above.

Report saved to: TEST_REPORT_AUTONOMOUS.md
```

---

## 📋 Автономность тестера

### Что делает тестер автономно

1. **Не требует ручного ввода команд**
   - Всё автоматизировано
   - Нужен только `npm run test:api`

2. **Проверяет все системы**
   - Функциональность (все фичи)
   - Валидация (все параметры)
   - Ошибки (все edge cases)
   - Производительность

3. **Генерирует отчёт**
   - Автоматически создаёт TEST_REPORT_AUTONOMOUS.md
   - Показывает проценты успешных тестов
   - Указывает что нужно исправить

### Преимущества

- ✅ Работает автономно — не трогает пользователя
- ✅ Полное покрытие всех фич
- ✅ Проверка ошибок
- ✅ Отчёт в формате Markdown
- ✅ Быстрый запуск — 1 команда

---

## 📋 Интеграция с разработкой

Тестер создан разработчиком (backend-dev) для использования тестером (tester).

**Процесс:**
1. Backend-dev создаёт код и tester.js
2. Tester запускает `npm run test:api`
3. Tester проверяет результаты
4. Если есть баги — debugger исправляет
5. Если всё ок — reviewer одобряет
6. Deployer деплоит на Vercel

---

## 📋 Автономный запуск (без пользователя)

Тестер создан чтобы тестировать API автономно без ручного ввода команд.

**Как это работает:**
- Тестер сам отправляет fetch запросы
- Тестер сам проверяет ответы
- Тестер сам генерирует отчёт
- Тестер сам сохраняет файл

**Пользователю нужно:**
- Установить зависимости
- Запустить сервер
- Запустить тестер
- Проверить результаты

---

**Дата:** 2026-02-21
**Версия:** 1.0.0
**Проект:** Image Crop API
