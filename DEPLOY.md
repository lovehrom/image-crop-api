# DEPLOY.md: Image Crop API

Дата деплоя: 2026-02-22

---

## Платформы

| Платформа | URL | Статус |
|-----------|-----|--------|
| GitHub | https://github.com/lovehrom/image-crop-api | ✅ Active |
| Vercel | https://image-crop-api-ten.vercel.app | ✅ Active |
| RapidAPI | — | 🟡 Planned |

---

## Переменные окружения

(не нужны)

---

## Как обновить деплой

### Автоматический деплой (рекомендуется)

**Каждый коммит в GitHub → автоматический деплой на Vercel**

```bash
# Измени код локально
cd K:\Работа\rapidapi.com\image-crop-api

# Коммит и пуш
git add .
git commit -m "Update code"
git push

# Vercel автоматически деплоит!
```

### Ручной деплой (если нужно)

```bash
cd K:\Работа\rapidapi.com\image-crop-api

# Деплой
vercel deploy

# Продакшн деплой
vercel --prod
```

---

## Возможные проблемы и решения

### Ошибка: "No existing credentials found"
**Решение:** `vercel login`

### Ошибка: "Command requires confirmation"
**Решение:** `vercel deploy --yes`

### Ошибка: "Failed to connect GitHub repository"
**Решение:** Подключи GitHub через UI:
https://vercel.com/lovehroms-projects/image-crop-api/settings/git

### Vercel деплоит из локальной директории вместо GitHub
**Решение:** Подключи GitHub:
```bash
vercel git connect https://github.com/lovehrom/image-crop-api.git --yes
```

---

## Проверка здоровья

```bash
# Health Check
curl https://image-crop-api-ten.vercel.app/health

# Ответ:
{
  "status": "healthy",
  "version": "1.0.0",
  "timestamp": "2024-01-01T00:00:00Z"
}
```

---

## API Endpoints

### GET /health
Проверка работоспособности API

### POST /crop
Обработка изображений

**Параметры:**
- `file` (required) — изображение (PNG/JPG, max 5MB)
- `width` (optional) — новая ширина (1-10000px)
- `height` (optional) — новая высота (1-10000px)
- `x` (optional) — X координата обрезки
- `y` (optional) — Y координата обрезки
- `cropWidth` (optional) — ширина области обрезки
- `cropHeight` (optional) — высота области обрезки
- `angle` (optional) — угол поворота (0-360°)
- `radius` (optional) — радиус скругления (10-50px)
- `format` (optional) — формат вывода (png, jpeg, webp)

---

## Полная инструкция по деплою

Детальная инструкция: `GITHUB_VERCEL_DEPLOY.md`

---

## Статус проекта

- ✅ Разработка завершена
- ✅ Тестирование пройдено (16/16, 100%)
- ✅ GitHub репозиторий создан
- ✅ Vercel деплой выполнен
- ✅ Production testing: 100% успеха
- ✅ GitHub подключён к Vercel
- 🟡 RapidAPI publication: Planned

---

## Следующие шаги

1. **RapidAPI** — публикация на платформу
2. **Документация** — обновление README.md
3. **Примеры** — создание примеров на разных языках
